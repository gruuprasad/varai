datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model UserTeam {
  id     String @id @default(cuid())
  userId String
  teamId String
}
